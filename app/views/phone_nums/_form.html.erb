<%= form_for(@phone_num) do |f| %>
  <% if @phone_num.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_num.errors.count, "error") %> prohibited this phone_num from being saved:</h2>

      <ul>
      <% @phone_num.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone_num %><br>
    <%= f.text_field :phone_num %>
  </div>
  <div class="field">
    <%= f.label :phone_verify_num %><br>
    <%= f.number_field :phone_verify_num %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
